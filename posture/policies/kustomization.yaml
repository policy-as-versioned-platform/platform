apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - stamp-posture.yaml           # MutatingPolicy — stamps posture from the claim
  - posture-trust-boundary.yaml  # ValidatingPolicy — rejects forged posture
