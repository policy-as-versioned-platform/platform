apiVersion: v1
kind: Pod
metadata:
  name: pod-claims
  labels: { "policy-as-versioned.dev/policy-version": "5.0.0" }
spec:
  containers: [{ name: app, image: nginx }]
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-forger
  labels:
    "policy-as-versioned.dev/policy-version": "5.0.0"
    "posture.acme.io/version": "9.9.9"
spec:
  containers: [{ name: app, image: nginx }]
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-other-version
  labels:
    "policy-as-versioned.dev/policy-version": "4.0.0"
    "posture.acme.io/version": "9.9.9"
spec:
  containers: [{ name: app, image: nginx }]
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-unversioned
spec:
  containers: [{ name: app, image: nginx }]
---
apiVersion: v1
kind: Pod
metadata:
  name: pod-unclaimed-forger
  labels: { "posture.acme.io/version": "5.0.0" }
spec:
  containers: [{ name: app, image: nginx }]
