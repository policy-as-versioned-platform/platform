apiVersion: v1
kind: Pod
metadata:
  labels:
    policy-as-versioned.dev/policy-version: 5.0.0
    policy-as-versioned.dev/root-attestation: SEC-PROBE
    posture.acme.io/tier: isolated
  name: corpus
spec:
  containers:
  - image: nginx:1.25
    name: app
    securityContext:
      capabilities:
        drop:
        - ALL
      readOnlyRootFilesystem: true
  securityContext:
    runAsNonRoot: false
